name: Render KeepAlive

on:
  schedule:
    - cron: "*/5 * * * *"   # каждые 7 минут
  workflow_dispatch:        # чтобы можно было запустить руками

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render Webhook
        run: curl -s -o /dev/null -w "%{http_code}" "https://bot-u0rw.onrender.com"
